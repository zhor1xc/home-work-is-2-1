let number = (i) => {
        if (Number.isInteger(result)) {
            console.log(true);
        } else {
            console.log(false);
    }
}
let key = prompt("Введите число");
let result = parseFloat(key);
number(result);